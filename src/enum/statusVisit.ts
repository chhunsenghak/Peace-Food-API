export enum StatusVisit {
    WAITING = "waiting",
    SERVED = "served",
    COMPLETED = "completed",
    SKIPPED = "skipped",
}

export enum StatusQueue {
    WAITING = "waiting",
    SERVED = "served",
    SKIPPED = "skipped",
}